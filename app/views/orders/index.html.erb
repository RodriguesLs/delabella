<h1 class="h1-new">Pedidos</h1>
<form class="form-content">
<table>
  <thead>
    <tr>
      <th class="th-index">Status</th>
      <th class="th-index space-c">Produto</th>
      <th class="th-index space-c">Cliente</th>
      <th colspan="10"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.status_i18n %></td>
        <td class="space"><%= order.products.pluck(:name).to_sentence %></td>
        <td><%= link_to 'Editar', edit_order_path(order) %></td>
        <td><%= link_to 'Encerrar', order %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Pedido', new_order_path, class:'btn' %>
<%= link_to 'Página Principal ', root_path, class:'btn' %>

</form>